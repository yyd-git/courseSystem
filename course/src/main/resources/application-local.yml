server:
  port: 8081
spring:
  application:
    name: catalog-service

  datasource:
    url: jdbc:mysql://localhost:3306/cs_course_db?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: "00000712"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848   # Nacos 服务地址
